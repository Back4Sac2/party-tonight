# UI/UX 플로우 다이어그램

## 📱 전체 앱 플로우

```
┌─────────────────────────────────────────────────────────────┐
│                    앱 시작 (/)                              │
│  ┌──────────────────┐  ┌──────────────────┐                │
│  │   게임 목록      │  │   선물 목록      │                │
│  │   (미리보기)     │  │   (미리보기)     │                │
│  └──────────────────┘  └──────────────────┘                │
└─────────────────────────────────────────────────────────────┘
         │                          │
         │                          │
         ▼                          ▼
┌──────────────────┐      ┌──────────────────┐
│  /games          │      │  /gifts          │
│  게임 목록 페이지 │      │  선물 관리 페이지 │
└──────────────────┘      └──────────────────┘
         │                          │
         │                          │
         │ 게임 클릭                 │ 선물/태그 추가
         │                          │
         ▼                          │
┌──────────────────┐                │
│  /games/[id]     │                │
│  게임 상세 페이지 │                │
│  ┌────────────┐  │                │
│  │ 게임 설명  │  │                │
│  └────────────┘  │                │
│  ┌────────────┐  │                │
│  │ 우승자 입력 │  │                │
│  │ [이름 입력] │  │                │
│  │ [선택 버튼] │  │                │
│  └────────────┘  │                │
└──────────────────┘                │
         │                          │
         │ 우승자 선택               │
         │                          │
         ▼                          │
┌──────────────────┐                │
│ /games/[id]/     │                │
│ select-tag       │                │
│                  │                │
│ ┌──────────────┐ │                │
│ │ 태그 목록    │ │                │
│ │ [태그1]      │ │                │
│ │ [태그2]      │ │                │
│ │ [태그3]      │ │                │
│ └──────────────┘ │                │
│                  │                │
│ ┌──────────────┐ │                │
│ │ 선물 목록    │ │                │
│ │ (선택한 태그)│ │                │
│ └──────────────┘ │                │
└──────────────────┘                │
                                    │
                                    ▼
                            ┌──────────────┐
                            │ 선물 획득 완료│
                            └──────────────┘
```

## 🎮 게임 플로우 상세

```
1. 게임 목록 페이지
   │
   ├─ 게임 카드 클릭
   │
   ▼
2. 게임 상세 페이지
   │
   ├─ 게임 설명 확인
   │
   ├─ 우승자 이름 입력
   │   └─ 예: "김철수"
   │
   ├─ [우승자 선택] 버튼 클릭
   │
   ▼
3. 게임 결과 생성
   │
   ├─ game_results 테이블에 저장
   │   └─ winner_name: "김철수"
   │
   ├─ 자동으로 태그 선택 페이지로 이동
   │
   ▼
4. 태그 선택 페이지
   │
   ├─ 태그 목록 표시
   │   ├─ [실용적인] (클릭 가능)
   │   ├─ [예쁜]
   │   ├─ [재미있는]
   │   └─ ...
   │
   ├─ 태그 클릭
   │   └─ 예: "실용적인" 클릭
   │
   ├─ 선택한 태그의 선물 목록 표시
   │   ├─ 선물 A (태그: 실용적인)
   │   ├─ 선물 B (태그: 실용적인)
   │   └─ ...
   │
   ├─ tag_selections 테이블에 저장
   │   └─ game_result_id + tag_id
   │
   └─ 선물 획득 처리
       └─ gifts.is_claimed = true
```

## 🎁 선물 관리 플로우

```
선물 관리 페이지 (/gifts)
│
├─ 좌측 패널: 선물 목록
│   │
│   ├─ [+ 추가] 버튼 클릭
│   │   │
│   │   ▼
│   │   선물 추가 폼
│   │   ├─ 이름: [입력]
│   │   ├─ 설명: [입력] (선택)
│   │   └─ [선물 추가] 버튼
│   │
│   └─ 선물 카드 목록
│       ├─ 선물 A
│       ├─ 선물 B
│       └─ ...
│
└─ 우측 패널: 태그 목록
    │
    ├─ [+ 추가] 버튼 클릭
    │   │
    │   ▼
    │   태그 추가 폼
    │   ├─ 이름: [입력]
    │   └─ [태그 추가] 버튼
    │
    └─ 태그 목록
        ├─ [실용적인]
        ├─ [예쁜]
        └─ ...
```

## 🔄 데이터 관계도

```
┌──────────┐
│ sessions │ (세션)
└────┬─────┘
     │
     ├──────────────┬──────────────┬──────────────┐
     │              │              │              │
     ▼              ▼              ▼              ▼
┌─────────┐   ┌─────────┐   ┌──────────────┐   ┌──────────────┐
│  gifts  │   │  tags   │   │ game_results │   │ game_results │
└────┬────┘   └────┬────┘   └──────┬───────┘   └──────┬───────┘
     │            │                │                  │
     │            │                │                  │
     └──────┬─────┘                │                  │
            │                      │                  │
            ▼                      ▼                  ▼
     ┌─────────────┐        ┌──────────────┐   ┌──────────────┐
     │ gift_tags   │        │   games      │   │tag_selections│
     │ (다대다)    │        │              │   │              │
     └─────────────┘        └──────────────┘   └──────────────┘
```

## 📱 모바일 화면 플로우

```
모바일 홈 화면
│
├─ [게임 목록] 섹션
│   └─ 스크롤 가능한 카드 리스트
│
├─ [선물 목록] 섹션
│   └─ 스크롤 가능한 카드 리스트
│
└─ 하단 네비게이션
    ├─ 홈
    ├─ 선물 관리
    └─ 게임
```

## 🎯 주요 사용자 시나리오

### 시나리오 1: 선물 추가 및 태그 설정

```
1. 사용자가 /gifts 페이지 접속
2. [+ 추가] 버튼 클릭
3. 선물 이름 입력: "스타벅스 기프트카드"
4. 설명 입력: "2만원권"
5. [선물 추가] 버튼 클릭
6. 태그 추가: "실용적인"
7. 선물에 태그 연결
```

### 시나리오 2: 게임 플레이 및 선물 획득

```
1. 사용자가 /games 페이지 접속
2. "가위바위보" 게임 클릭
3. 게임 설명 확인
4. 우승자 이름 입력: "김철수"
5. [우승자 선택] 버튼 클릭
6. 태그 선택 페이지로 이동
7. "실용적인" 태그 클릭
8. 해당 태그의 선물 목록 확인
9. 선물 자동 획득 처리
```

## 🎨 UI 컴포넌트 계층 구조

```
Page Component
│
├─ Layout Components
│   ├─ Header
│   └─ Footer
│
└─ Domain Components
    ├─ Game Domain
    │   ├─ GameList
    │   │   └─ GameCard (반복)
    │   ├─ GameDetail
    │   ├─ WinnerSelector
    │   └─ TagSelector
    │       ├─ TagList
    │       └─ GiftList
    │
    ├─ Gift Domain
    │   ├─ GiftList
    │   │   └─ GiftCard (반복)
    │   └─ GiftForm
    │
    └─ Tag Domain
        ├─ TagList
        └─ TagForm
            │
            └─ UI Components (재사용)
                ├─ Button
                ├─ Card
                ├─ Input
                └─ Badge
```

## 🔔 상태 업데이트 플로우

```
사용자 액션
    │
    ▼
Component Event Handler
    │
    ▼
React Query Mutation Hook
    │
    ├─ Optimistic Update (선택적)
    │
    ▼
Repository Method
    │
    ▼
Supabase API Call
    │
    ├─ Success
    │   │
    │   ▼
    │   React Query Cache Invalidation
    │   │
    │   ▼
    │   UI 자동 리렌더링
    │
    └─ Error
        │
        ▼
        Error State 표시
```

## 📊 화면별 주요 컴포넌트 목록

### 홈 페이지 (/)

- `GameList` - 게임 목록 표시
- `GiftList` - 선물 목록 표시
- `Header` - 네비게이션
- `Footer` - 푸터

### 선물 관리 페이지 (/gifts)

- `GiftList` - 선물 목록
- `GiftForm` - 선물 추가 폼
- `TagList` - 태그 목록
- `TagForm` - 태그 추가 폼
- `Card` (UI) - 카드 컨테이너

### 게임 목록 페이지 (/games)

- `GameList` - 게임 목록
- `GameCard` (내부) - 개별 게임 카드

### 게임 상세 페이지 (/games/[id])

- `GameDetail` - 게임 정보 표시
- `WinnerSelector` - 우승자 입력 폼
- `Card` (UI) - 카드 컨테이너

### 태그 선택 페이지 (/games/[id]/select-tag)

- `TagSelector` - 태그 선택 컨테이너
- `TagList` - 태그 목록 (선택 가능)
- `GiftList` - 선택한 태그의 선물 목록
- `Card` (UI) - 카드 컨테이너
